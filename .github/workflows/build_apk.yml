name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.0'
          channel: stable

      - name: Regenerate android folder only
        run: |
          cp pubspec.yaml /tmp/pubspec_backup.yaml
          cp -r lib /tmp/lib_backup
          rm -rf android
          flutter create --org com.example --project-name ccc .
          cp /tmp/pubspec_backup.yaml pubspec.yaml
          rm -rf lib
          cp -r /tmp/lib_backup lib

      - name: Add google-services.json
        env:
          GOOGLE_SERVICES: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: |
          echo "$GOOGLE_SERVICES" > android/app/google-services.json

      - name: Add Firebase and NDK config
        run: |
          sed -i 's/compileSdk = [0-9]*/compileSdk = 34/' android/app/build.gradle.kts
          sed -i '/compileSdk/a\    ndkVersion = "27.0.12077973"' android/app/build.gradle.kts
          cat >> android/build.gradle.kts << 'ENDOFFILE'
          buildscript {
              repositories {
                  google()
                  mavenCentral()
              }
              dependencies {
                  classpath("com.google.gms:google-services:4.4.1")
              }
          }
          ENDOFFILE
          echo 'apply(plugin = "com.google.gms.google-services")' >> android/app/build.gradle.kts

      - name: Build APK
        run: |
          flutter pub get
          flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
